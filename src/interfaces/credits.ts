export interface ICredits {
  name: string;
  nameLink: string;
  author?: string;
  authorLink?: string;
  uploader?: string;
  uploaderLink?: string;
  license: string;
  licenseLink: string;
  changes: string;
  fieldName: string;
}